export const PageUtil = (totalCount:number)=>{
    
    return 
}
// {
//     private currentPage: number;// 当前页
//     private pageSize: number;// 每页大小
//     private totalCount: number;// 总记录数
//     private totalPage: number;// 总页数

//     /**
//      * 
//      * @param total 总记录数
//      * @param pageSize 每页条数
//      */
//     constructor(total: number, pageSize: number)
//     {
//         this.currentPage = 1;
//         this.totalCount = total;
//         this.pageSize = pageSize;
//         this.totalPage = total % pageSize === 0 ? total / pageSize : Math.ceil((total / pageSize));
//     };
    /**
     * currentPage 返回当前页码
     */
    // public get currentPage()
    // {
    //     this.totalPage = this.totalCount % this.pageSize === 0 ? this.totalCount / this.pageSize : Math.ceil((this.totalCount / this.pageSize));
    //     return this.currentPage;
    // }
    /**
     * 
     */
    // public set currentPage(currentPage: number)
    // {
    //     this.currentPage = currentPage;
    // }
    /**
     * pageSize 每页条数
     */
    // public get pageSize()
    // {
    //     return this.pageSize;
    // }
    /**
     * set count
     */
    // public set pageSize(pageSize: number)
    // {
    //     this.pageSize = pageSize;
    // }
    /**
     * pageSize 每页条数
     */
    // public get totalCount()
    // {
    //     return this.totalCount;
    // }
    /**
     * set count
     */
    // public set totalCount(totalCount: number)
    // {
    //     this.totalCount = totalCount;
    // }
    /**
     * pageSize 总页数
     */
    // public get totalPage()
    // {
    //     this.totalPage = this.totalCount % this.pageSize === 0 ? this.totalCount / this.pageSize : Math.ceil(this.totalCount / this.pageSize);
    //     return this.totalPage;
    // }
// }